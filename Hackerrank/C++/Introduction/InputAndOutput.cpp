#include <iostream>

using std::cout;
using std::cin;
using std::endl;

int main() {
    int num1, num2, num3, sum;
    // Get values
    cin >> num1 >> num2 >> num3;
    // Add values
    sum = num1 + num2 + num3;
    // Print sum
    cout << sum << endl;

    return 0;
}
